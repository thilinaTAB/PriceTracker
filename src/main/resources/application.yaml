spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pricetracker
    username: postgres
    password: post123
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080